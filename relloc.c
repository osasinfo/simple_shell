#include "shell.h"

/**
 * realloc - allocate new memory for _getline
 * @int old - Old memory space
 * @int news - allocate space for new memory
 * Retune - return new
 */

void *_realloc(void *_getline , int old , int news);

{
void *temp, int old, int news

if (old ==  news)
{
return (_getline);
}
if (old == NULL)
{
temp = malloc(news);
return(_getline);
}
	    
while (_getline == 0 && news == NULL )
{
news = realloc(b*, sizeof(int buFsize) *1000);
temp = malloc(sizeof(temp));

old->temp;
temp-> news;
}
free (temp);
return(news)
}	
